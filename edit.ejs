<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirection</title>
</head>
<body>
    <a href="/all">Todos os links</a>
    <h1>edit Link</h1>
    <form action="/edit/<%=body.id%>" method="POST">
        <input class="field" name="title" placeholder="Title" value="<%=body.title%>">
        <input class="field" name="description" placeholder="Description" value="<%=body.description%>">
        <input class="field" name="url" placeholder="URL" value="<%=body.url%>">
        <button>edit</button>

        <%if(error) {%>
        <h3><%=error.message%></h3>
        <%}%>
</body>
</html>