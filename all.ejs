<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All links</title>
</head>
<body>
    <h1>Links</h1>
    <a href="/">adcionar link</a>
    <% for(let i=0; i < links.length; i++) {%>
        <div id="<%=links[i]._id%>">
            <h2><%=links[i].title%></h2>
            <div><%=links[i].description%></div>
            <div><%=links[i].url%></div>
            <div><%=links[i].click%>clicks</div>
            <button onclick="deleteLink('<%=links[i]._id%>')">Delete</button>
            <a href="/edit/<%=links[i]._id%>">edit</a>
        </div>
    <%}%>
    <script>
        function deleteLink(id) {
            fetch("/"+ id, {method: "DELETE"}).then(res => {
                res.text().then(linkId => {
                    document.getElementById(linkId).remove();
                })
            })
        }
    </script>
</body>
</html>